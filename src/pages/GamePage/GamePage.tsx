import { classNames } from '@/shared/utils/classNames/classNames';
import cls from './GamePage.module.scss';

export const GamePage = () => {
    return (
        <div className={classNames(cls.GamePage)}>GamePage/</div>
    );
};
